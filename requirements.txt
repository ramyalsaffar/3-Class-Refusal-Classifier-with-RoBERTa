# Dual RoBERTa Classifiers: 3-Class Refusal Taxonomy & Binary Jailbreak Detection
# Python 3.8+ required

# Core ML/DL
torch>=2.0.0
transformers>=4.30.0
numpy>=1.24.0
scipy>=1.10.0
scikit-learn>=1.3.0

# Data Processing
pandas>=2.0.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Progress Bars
tqdm>=4.65.0

# LLM APIs
openai>=1.0.0
anthropic>=0.5.0
google-generativeai>=0.3.0
tiktoken>=0.5.0  # Token counting for API usage tracking

# Production API
fastapi>=0.100.0
uvicorn>=0.23.0
pydantic>=2.0.0

# Database
psycopg2-binary>=2.9.0

# AWS (optional - use requirements-aws.txt for AWS deployment)
# boto3>=1.28.0  # Moved to requirements-aws.txt to avoid duplication

# PDF Reports
reportlab>=4.0.0

# Interpretability (SHAP - optional, use requirements-interpretability.txt)
# shap>=0.42.0  # Moved to requirements-interpretability.txt (~500MB with dependencies)

# Optional: Hyperparameter tuning (for Phase 3)
# optuna>=3.0.0

# Optional: Experiment tracking (for Phase 4)
# mlflow>=2.5.0
# wandb>=0.15.0
